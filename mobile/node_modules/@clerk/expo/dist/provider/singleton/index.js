

var src_provider_singleton_singleton = require("./singleton");
Object.keys(src_provider_singleton_singleton).forEach(function (k) {
  if (k !== 'default' && !Object.prototype.hasOwnProperty.call(exports, k)) Object.defineProperty(exports, k, {
    enumerable: true,
    get: function () { return src_provider_singleton_singleton[k]; }
  });
});
