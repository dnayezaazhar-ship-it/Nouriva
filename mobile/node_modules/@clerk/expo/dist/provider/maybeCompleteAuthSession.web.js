Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

//#region src/provider/maybeCompleteAuthSession.web.ts
/**
* Web-only implementation. Resolved by Metro / the bundler in place of
* `maybeCompleteAuthSession.ts` when targeting `web`.
*
* Must stay synchronous during render so the OAuth/SSO redirect URL is caught
* before children mount.
*/
function maybeCompleteAuthSession() {
	try {
		require("expo-web-browser").maybeCompleteAuthSession();
	} catch (e) {}
}

//#endregion
exports.maybeCompleteAuthSession = maybeCompleteAuthSession;
//# sourceMappingURL=maybeCompleteAuthSession.web.js.map