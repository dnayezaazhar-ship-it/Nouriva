Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
let src_utils_errors = require("../utils/errors");

//#region src/biometric-credentials/useBiometricCredentials.ts
const unsupportedAvailability = {
	isAvailable: false,
	unavailableReason: "unsupported_platform"
};
function unsupported() {
	return src_utils_errors.errorThrower.throw("Biometric credentials are currently only available on iOS and Android.");
}
function rejectUnsupported() {
	return Promise.resolve().then(unsupported);
}
const biometricCredentials = Object.freeze({
	getAvailability: () => Promise.resolve(unsupportedAvailability),
	list: rejectUnsupported,
	enroll: rejectUnsupported,
	revoke: rejectUnsupported,
	signIn: rejectUnsupported
});
/**
* Accesses biometric credential enrollment and sign-in.
*
* Biometric credentials are currently supported on iOS and Android.
*/
function useBiometricCredentials() {
	return biometricCredentials;
}

//#endregion
exports.useBiometricCredentials = useBiometricCredentials;
//# sourceMappingURL=useBiometricCredentials.js.map