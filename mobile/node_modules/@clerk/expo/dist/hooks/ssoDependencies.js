Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
let src_utils_errors = require("../utils/errors");

//#region src/hooks/ssoDependencies.ts
function loadSSODependencies() {
	let AuthSessionModule;
	let WebBrowserModule;
	try {
		AuthSessionModule = require("expo-auth-session");
		WebBrowserModule = require("expo-web-browser");
	} catch (err) {
		return src_utils_errors.errorThrower.throw(`Unable to load expo-auth-session and expo-web-browser, which are required for SSO: ${err instanceof Error ? err.message : "Unknown error"}. If they are not installed, run: npx expo install expo-auth-session expo-web-browser`);
	}
	return {
		AuthSession: AuthSessionModule,
		WebBrowser: WebBrowserModule
	};
}

//#endregion
exports.loadSSODependencies = loadSSODependencies;
//# sourceMappingURL=ssoDependencies.js.map