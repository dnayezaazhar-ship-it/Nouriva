Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
const require_runtime = require('../_virtual/_rolldown/runtime.js');
let react = require("react");
let src_utils_native_module = require("../utils/native-module");
let react_native = require("react-native");
let src_specs_NativeClerkAuthView = require("../specs/NativeClerkAuthView");
src_specs_NativeClerkAuthView = require_runtime.__toESM(src_specs_NativeClerkAuthView);
let react_jsx_runtime = require("react/jsx-runtime");

//#region src/native/AuthView.tsx
/**
* A pre-built native authentication component that handles sign-in and sign-up flows.
*
* `AuthView` renders inline within your React Native view hierarchy, powered by:
* - **iOS**: clerk-ios (SwiftUI) - https://github.com/clerk/clerk-ios
* - **Android**: clerk-android (Jetpack Compose) - https://github.com/clerk/clerk-android
*
* After authentication completes, the session is automatically synced with the JS SDK.
* Use `useAuth()`, `useUser()`, or `useSession()` to react to authentication
* state changes.
*
* To push the auth flow onto your own navigation stack, hide the route's header and
* pass `onHostBack` so Clerk's own chrome takes over.
*
* @example
* ```tsx
* import { AuthView } from '@clerk/expo/native';
* import { useAuth } from '@clerk/expo';
*
* export default function SignInScreen() {
*   const { isSignedIn } = useAuth();
*
*   useEffect(() => {
*     if (isSignedIn) router.replace('/home');
*   }, [isSignedIn]);
*
*   return <AuthView />;
* }
* ```
*
* @see {@link https://clerk.com/docs/components/authentication/sign-in} Clerk Sign-In Documentation
*/
function AuthView({ logo, mode = "signInOrUp", isDismissible = true, logoMaxHeight, onDismiss, onHostBack }) {
	const handleAuthEvent = (0, react.useCallback)((event) => {
		if (event.nativeEvent.type === "dismissed") onDismiss?.();
	}, [onDismiss]);
	if (!src_utils_native_module.isNativeSupported || !src_specs_NativeClerkAuthView.default) return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(react_native.View, {
		style: {
			flex: 1,
			justifyContent: "center",
			alignItems: "center"
		},
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(react_native.Text, {
			style: {
				fontSize: 16,
				color: "#666"
			},
			children: !src_utils_native_module.isNativeSupported ? "Native AuthView is only available on iOS and Android" : "Native AuthView requires the @clerk/expo plugin. Add \"@clerk/expo\" to your app.json plugins array."
		})
	});
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(src_specs_NativeClerkAuthView.default, {
		style: { flex: 1 },
		mode,
		isDismissible,
		logoMaxHeight,
		hostBackButton: !!onHostBack,
		onAuthEvent: handleAuthEvent,
		onHostBack: onHostBack ? () => onHostBack() : void 0,
		children: logo ? /* @__PURE__ */ (0, react_jsx_runtime.jsx)(react_native.View, {
			collapsable: false,
			style: { alignSelf: "flex-start" },
			children: logo
		}) : null
	});
}

//#endregion
exports.AuthView = AuthView;
//# sourceMappingURL=AuthView.js.map