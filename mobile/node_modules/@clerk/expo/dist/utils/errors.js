Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
let _clerk_shared_error = require("@clerk/shared/error");
let src_utils_runtime = require("./runtime");
let _clerk_shared_proxy = require("@clerk/shared/proxy");

//#region src/utils/errors.ts
const errorThrower = (0, _clerk_shared_error.buildErrorThrower)({ packageName: "@clerk/expo" });
function assert(condition, thrower) {
	if (!condition) thrower();
}
const assertValidProxyUrl = (proxyUrl) => {
	if (!proxyUrl) return;
	if ((0, src_utils_runtime.isNative)()) {
		assert(typeof proxyUrl === "string", () => errorThrower.throw("`proxyUrl` must be a string in non-browser environments."));
		if (!(0, _clerk_shared_proxy.isHttpOrHttps)(proxyUrl)) errorThrower.throw("`proxyUrl` must be an absolute URL in non-browser environments.");
	}
};

//#endregion
exports.assertValidProxyUrl = assertValidProxyUrl;
exports.errorThrower = errorThrower;
//# sourceMappingURL=errors.js.map