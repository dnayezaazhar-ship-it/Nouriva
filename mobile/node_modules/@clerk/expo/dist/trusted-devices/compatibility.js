Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

//#region src/trusted-devices/compatibility.ts
function toBiometricCredentialEnrollmentParams(params) {
	if (!params) return;
	const { deviceName, ...rest } = params;
	return {
		...rest,
		name: deviceName
	};
}
function toTrustedDevices(biometricCredentials) {
	return {
		...biometricCredentials,
		enroll: (params) => biometricCredentials.enroll(toBiometricCredentialEnrollmentParams(params))
	};
}

//#endregion
exports.toTrustedDevices = toTrustedDevices;
//# sourceMappingURL=compatibility.js.map