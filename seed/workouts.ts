// Dedicated workout seed entrypoint. The collections remain part of the shared
// seedData object so the existing seed runner and validation stay unchanged.
export { exercises, workouts } from "./data";
